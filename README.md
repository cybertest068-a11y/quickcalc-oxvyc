# QuickCalc

> Amake akta calculator app baniye daw
Ami tomake features bolchi tumi amake prompt likhe debe ok?
Amar akta calculator app baniye daw app open korle 3 sec loding hobe tar por app open hobe ar calculator app a button tiple

**Built with [Nova AI](https://nova-spark-duo.lovable.app)** — a 2-in-1 AI companion that chats and ships installable web apps.

## 🚀 Live Demo
Once GitHub Pages finishes building (~1 minute), open the Pages URL from your repo's **Settings → Pages** section.

## 📱 Install as a PWA
1. Open the live URL in Chrome / Safari on your phone
2. Tap the browser menu → **Add to Home Screen**
3. The app installs like a native app 🎉

## 🗂 Structure
```
index.html          → app shell
styles.css          → dark, mobile-first UI
app.js              → all logic (uses localStorage)
manifest.json       → PWA metadata
service-worker.js   → offline cache
icon.svg            → app icon
```

## 🛠 Local Development
Just open `index.html` in a browser — no build step needed. For full PWA testing, serve it over HTTPS (e.g. `npx serve`).

---
Made with 🖤 by Nova AI
